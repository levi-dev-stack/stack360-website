// import type { VerifyAuthRoute } from '@/schema/dynamic-route';

export const routes = Object.freeze({
  root: '/',
  ui: {
    index: '/',
  },
  api: {
    health: '/api/health',
    ip: '/api/ip',
  },
});
